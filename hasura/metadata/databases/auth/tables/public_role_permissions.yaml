table:
  name: role_permissions
  schema: public
insert_permissions:
  - role: Super-Admin
    permission:
      check: {}
      columns:
        - id
        - permission_id
        - role_key
select_permissions:
  - role: Super-Admin
    permission:
      columns:
        - id
        - permission_id
        - role_key
      filter: {}
update_permissions:
  - role: Super-Admin
    permission:
      columns:
        - id
        - permission_id
        - role_key
      filter: {}
      check: {}
delete_permissions:
  - role: Super-Admin
    permission:
      backend_only: false
      filter: {}
